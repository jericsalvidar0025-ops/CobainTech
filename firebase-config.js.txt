// firebase-config.js
// Paste this file in the same folder as your HTML files.
// This file initializes Firebase (compat) and exposes convenient globals:
//   window.auth, window.db, window.storage

// Replace nothing — this is your project's config (already filled).
const firebaseConfig = {
  apiKey: "AIzaSyBDYLrPmmPvu0PfEa6qIDv4kgyHQM8mQ54",
  authDomain: "cobaintech-aff85.firebaseapp.com",
  projectId: "cobaintech-aff85",
  storageBucket: "cobaintech-aff85.appspot.com", // corrected
  messagingSenderId: "70064253311",
  appId: "1:70064253311:web:fd7b0680f82143f308124a",
  measurementId: "G-1BEEG4EVBR"
};

// Initialize Firebase (compat)
if (typeof firebase !== 'undefined') {
  try {
    firebase.initializeApp(firebaseConfig);
    // convenience refs
    window.auth = firebase.auth();
    window.db = firebase.firestore();
    window.storage = firebase.storage();
    console.log('Firebase initialized (compat).');
  } catch (err) {
    console.error('Firebase init error', err);
  }
} else {
  console.error('Firebase SDK not loaded. Make sure SDK scripts are included in HTML before firebase-config.js');
}
